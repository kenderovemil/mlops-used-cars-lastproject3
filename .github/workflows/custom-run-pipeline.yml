name: run-pipeline

on:
  workflow_call:
    inputs:
      parameters-file:
        required: true
        type: string
      resource_group:
        required: true
        type: string
      workspace_name:
        required: true
        type: string
      job-name:
        required: true
        type: string
      auth-type:
        required: false
        type: string
    secrets:
      AZURE_CLIENT_ID:
        required: true
      AZURE_TENANT_ID:
        required: true
      AZURE_SUBSCRIPTION_ID:
        required: true

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          auth-type: ${{ inputs.auth-type }}
          
      - name: install-extension
        run: az extension add -n ml -y

      - name: Update Azure ML extension
        run: az extension update -n ml

      - name: Validate resource group and workspace
        run: |
          echo "Using resource group: ${{ inputs.resource_group }}"
          echo "Using workspace: ${{ inputs.workspace_name }}"
          az group show --name ${{ inputs.resource_group }} || { echo "❌ Resource group not found"; exit 1; }
          az ml workspace show --name ${{ inputs.workspace_name }} --resource-group ${{ inputs.resource_group }} || { echo "❌ Workspace not found"; exit 2; }

      - name: Run ML pipeline
        run: |
          echo "🔧 Submitting pipeline from file: ${{ inputs.parameters-file }}"
          run_id=$(az ml job create \
            --file ${{ github.workspace }}/${{ inputs.parameters-file }} \
            --resource-group ${{ inputs.resource_group }} \
            --workspace-name ${{ inputs.workspace_name }} \
            --query name -o tsv)

          if [[ -z "$run_id" ]]; then
            echo "❌ Job creation failed"
            exit 3
          fi

          echo "🚀 Job submitted: $run_id"
          az ml job show -n "$run_id" --resource-group ${{ inputs.resource_group }} --workspace-name ${{ inputs.workspace_name }} --web

          status=$(az ml job show -n "$run_id" --resource-group ${{ inputs.resource_group }} --workspace-name ${{ inputs.workspace_name }} --query status -o tsv)

          if [[ -z "$status" ]]; then
            echo "❌ Status query failed"
            exit 4
          fi

          running=("NotStarted" "Queued" "Starting" "Preparing" "Running" "Finalizing" "CancelRequested")
          while [[ ${running[*]} =~ $status ]]; do
            echo "⏳ Status: $status"
            sleep 15
            status=$(az ml job show -n "$run_id" --resource-group ${{ inputs.resource_group }} --workspace-name ${{ inputs.workspace_name }} --query status -o tsv)
          done

          echo "✅ Final status: $status"
          if [[ "$status" != "Completed" ]]; then
            echo "❌ Training Job failed or canceled"
            exit 5
          fi
