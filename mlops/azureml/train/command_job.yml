$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
type: command

# 🛠️ Командата, която ще се изпълни
command: >-
  python train.py
  --data ${{inputs.training_data}}
  --n_estimators 200
  --max_depth 10
  --model_output ${{outputs.model_output}}

# 🌍 Environment (регистриран в Azure ML)
environment: azureml:train-env:5

# 📂 Входове
inputs:
  training_data: azureml:used-cars-data:2.55654

# 📦 Изходи
outputs:
  model_output:
    type: uri_folder
    mode: rw_mount

# ⚙️ Compute target
compute: azureml:lastprojectcompute

# 🧪 Метаданни за експеримента
experiment_name: project_III_MLOPS
display_name: used-cars-train-job
description: "Training job for SkyCore — RandomForest on used_cars dataset"

# 🏷️ Тагове за проследяване
tags:
  project: SkyCore
  owner: Emil
  purpose: training
  dataset_version: "2.55654"
